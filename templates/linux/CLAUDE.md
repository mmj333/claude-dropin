# Linux field session

Stub — Linux is primarily the dev/smoke-test target for this tool. The
primary field target is Windows (see `templates/windows/CLAUDE.md` for the
real diagnostic reference).

Rules of engagement on any field box:

1. Inspect before modifying.
2. Leave nothing behind — all artifacts go in `work/`.
3. Document findings in `work/<ticket>.md`.
